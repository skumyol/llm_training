# Schema

The schema reference is maintained at `docs/source/schema.md`.

The paper-level explanation of the schema is in `docs/source/technical_paper.md`, and the generated list of model heads is in `docs/source/model_registry.md`.
